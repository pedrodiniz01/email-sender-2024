spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/emailsender
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  mail:
    host: smtp.office365.com
    port: 587
    username: gitomotv24451@outlook.com
    password: L4bO1W8k7o2ERik
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect